{
    "title": "NPCs",
    "description": "NPC data",
    "type": "object",
    "properties": {
        "name": {
            "type": "string"
        },
        "age": {
            "type": "integer",
            "minimum": 0,
            "maximum": 1000
        },
        "inventory": {
            "type": "array",
            "items": {
                "$ref": "./item.schema.json"
            }
        }
    },
    "required": ["name"]
}
